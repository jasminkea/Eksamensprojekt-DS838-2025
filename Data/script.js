// Select the component with a specific ID
const element = document.getElementById('square1');

// Do something with the selected element
if (element) {
    element.textContent = 'Hello, World!';
    element.style.color = 'blue';
}